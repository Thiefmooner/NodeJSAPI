/**
 *           http版本号      响应状态码（200 403 500）
 *   响应行 ：HTTP/1.1       200                       OK
 *   响应头 ：Content-Type :text/html; charset=utf-8       (还有一些键值对Cache-Control等)
 *   响应体 ： 格式灵活，常见有，HTML，CSS，JS等
 */